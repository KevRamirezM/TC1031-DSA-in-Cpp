#include <iostream>
#include <string>

bool palindrome(char str[], int i, int f){
    if (i>=f){
        return true;
    }
    else if{
        
    }
    else{
        if (char[i] || char[f])
    }
}